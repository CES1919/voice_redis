# Redis 服务器配置
# 绑定所有接口，由 Docker 端口映射控制外部访问
bind 0.0.0.0

# AOF 持久化
appendonly yes
appendfsync everysec

# 内存限制
maxmemory 256mb
maxmemory-policy noeviction

# ACL 文件路径（容器内共享卷路径）
aclfile /usr/local/etc/redis/acl/users.acl

# 禁用 protected-mode（使用 ACL 认证即可）
protected-mode no

# 日志级别
loglevel notice
