# Redis 服务器配置
# 绑定所有接口，由 Docker 端口映射控制外部访问
bind 0.0.0.0

# 禁用 protected-mode（使用 ACL 认证即可）
protected-mode no

# ACL 文件路径（容器内路径，由 start_redis.sh 运行时生成）
aclfile /usr/local/etc/redis/acl/users.acl

# AOF 持久化
appendonly yes
appendfsync everysec

# 日志级别
loglevel notice
